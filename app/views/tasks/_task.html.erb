<li class="tasks__list-task" id="task-<%= task.id %>">
  <%= check_box :task, :done, {:checked => @task.done, class: "check_box"} %>
  <%= task.name  %>
  <%= link_to task_path(task),
              method: :delete,
              data: { confirm: '削除しますか?', remote: true } do %>
    <i class="far fa-trash-alt"  aria-hidden="true"></i>
  <% end  %>            
</li>
